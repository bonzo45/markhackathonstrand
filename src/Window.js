import React, { useState, Fragment, useCallback } from 'react';

function Window() {
  return (
    <div style={{
        "width": "100%",
        "height": "100%",
        "background-color": "red",
    }}>
        A window!
    </div>
  );
}

export default Window;
