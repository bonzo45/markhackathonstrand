import React, { useState, Fragment, useCallback } from 'react';

function Window() {
  return (
    <div style={{
        "width": "100%",
        "height": "100%",
        "backgroundColor": "#e1f7ff",
        "color": "black",
    }}>
    </div>
  );
}

export default Window;
